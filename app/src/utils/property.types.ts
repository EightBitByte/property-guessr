export type PropertyData = {
    address: string,
    assessed_total: string,
    build_year: string,
    last_sale_date: string,
    last_sale_price: string,
    latitude: number,
    longitude: number
}